# Contributing

PRs and ideas are welcome. The minimum bar for merging: run `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test` before opening a PR so CI passes.
