# Tasks — YouTube Transcript Adapter

## Prerequisites

- [x] Verify `corpora/video/urls.txt` exists with the supplied YouTube URLs, and `corpora/default/urls.txt` includes the same URLs for default dogfood coverage.
- [x] Add adapter module structure and expose `adapters` from `src/lib.rs`; prefer disjoint files (`src/adapters/youtube/mod.rs`, `url.rs`, `transcript.rs`, `innertube.rs`) if using parallel subagents.
- [x] Add `quick-xml` to `Cargo.toml` for transcript XML parsing and verify `cargo check` succeeds.

## Adapter URL handling

- [x] [parallel-safe if isolated in `url.rs`] Implement YouTube URL classification: `NotYoutube`, supported video URL, unsupported YouTube URL.
- [x] [parallel-safe if isolated in `url.rs`] Implement video ID extraction for `youtube.com/watch?v=...`, `www.youtube.com/watch?v=...`, `youtu.be/...`, `youtube.com/shorts/...`, and `www.youtube.com/shorts/...`.
- [x] [parallel-safe if isolated in `url.rs`] Preserve normalized user-requested URL for storage; do not canonicalize all URL forms to `watch?v=`.
- [x] [parallel-safe if isolated in `url.rs`] Support watch URLs with extra query params such as `t`, `si`, `feature`, or `list` when a valid `v` param exists.
- [x] [parallel-safe if isolated in `url.rs`] Implement explicit unsupported handling for embed URLs, playlist/channel/search/non-video YouTube URLs, missing video IDs, malformed video IDs, YouTube-like unsupported hosts, and unsupported schemes.
- [x] [parallel-safe if isolated in `url.rs`] Add unit tests for supported URL normalization and video ID extraction, including extra query params, trailing slashes, and uppercase/lowercase hosts.
- [x] [parallel-safe if isolated in `url.rs`] Add unit tests for unsupported YouTube URL forms and non-YouTube URL classification.

## Transcript parsing and caption selection

- [x] [parallel-safe if isolated in `transcript.rs` or `innertube.rs`] Define partial caption track model and implement English track selection: accept `en`/`en-*`, prefer manual English, then autogenerated English, never non-English/translation fallback.
- [x] [parallel-safe] Add unit tests for caption track selection, including `en`, `en-*`, ASR/manual priority, and no-English failure.
- [x] [parallel-safe if isolated in `transcript.rs`] Implement `quick-xml` transcript parsing for current `timedtext format="3"` shape using `<p>` and nested `<s>` text nodes.
- [x] [parallel-safe if isolated in `transcript.rs`] Implement transcript XML parsing for legacy/simple `<transcript><text start="...">...</text></transcript>` shape.
- [x] [parallel-safe if isolated in `transcript.rs`] Handle transcript edge shapes: text directly inside `<p>`, nested styling tags, blank layout paragraphs, malformed XML, and valid XML with no text nodes.
- [x] [parallel-safe if isolated in `transcript.rs`] Implement transcript text cleanup: XML/entity decoding, possible double-encoded entity handling, whitespace normalization, empty segment removal, and plain prose formatting without timestamps or links.
- [x] [parallel-safe if isolated in `transcript.rs`] Add parser fixture tests for `timedtext` XML, simple `text` XML, spacing across `<s>` nodes, entity/double-entity decoding, empty transcript rejection, malformed XML rejection, and no timestamp output.

## InnerTube fetch

- [x] Define `YoutubeError` adapter error type with useful Display messages for unsupported URL, no English captions, empty transcript, parse failure, network failure, and player failure.
- [x] Define partial InnerTube player response structs for only needed fields: playability status, video title, caption tracks; missing nested fields must fail cleanly.
- [x] Implement synchronous `reqwest::blocking` InnerTube player metadata request with Android client context and bounded timeout.
- [x] Implement synchronous `reqwest::blocking` caption XML fetch from selected `baseUrl` with bounded timeout.
- [x] Implement adapter orchestration: supported URL → video ID → player response → title fallback → English track → transcript XML → `YoutubeTranscriptDocument`.
- [x] Add ignored network test for one supplied captioned video URL that verifies title/body are non-empty and body lacks YouTube page chrome.

## Collect integration

- [x] Refactor `src/collect.rs` to introduce a shared internal document write helper used by both HTML extraction and adapter-produced documents.
- [x] Add dedicated `CollectError::Youtube` wrapping adapter failures and ensure CLI output remains concise/useful.
- [x] Route YouTube URLs through the adapter before generic HTML fetch in `collect_url`.
- [x] Ensure unsupported YouTube URLs fail gracefully without falling through to generic HTML collection.
- [x] Ensure adapter failures never write a leaf file or append `index.jsonl`.
- [x] Preserve existing `collect_html` behavior and public testability for non-network HTML fixtures.
- [x] Add collect-level tests for unsupported YouTube embed URL rejection with no file/index writes.
- [x] Add collect-level tests for ordinary HTML collection unchanged after the write-helper refactor.
- [x] Add duplicate detection coverage for normalized YouTube URLs using the shared write/index path; keep exact-match semantics, including separate entries for different URL forms of the same video.

## Validation

- [x] Run `cargo fmt`.
- [x] Run `cargo test` and fix failures.
- [x] Run `cargo clippy --all-targets --all-features -- -D warnings` and fix warnings.
- [x] Run ignored YouTube network test explicitly and record whether current network environment can fetch transcripts.
- [x] Run `./scripts/dogfood-collect video` and inspect failures for adapter/user-facing error quality.
- [x] Confirm `bo collect` on at least one supported YouTube URL writes a markdown leaf with video title, normalized URL frontmatter, plain transcript body, and one index entry.
