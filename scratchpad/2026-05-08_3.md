# 2026-05-08 (3)

## Session summary

Replaced the compile agent loop with a structured-output pipeline. Full cycle:
discussion → ADR → speckit (spec/plan/data-model/research/tasks/analysis) → implement → dogfood.

## What shipped

- ADR-001: deterministic pipelines over internal agent loops
- `engine/agent/` deleted (1,424 LOC), replaced with `engine/llm/` (212 LOC)
- `cli/compile.rs` — flat pipeline, single structured-output LLM call
- Validation: ≥2 leaf gate, post-slugification uniqueness, truncation detection
- Prompt: XML-fenced documents, explicit cross-cutting rules
- Dogfood confirmed working: 5 docs (gpt-4o), 53 docs (gpt-4.1-mini)

## Open items

- [ ] Merge `refactor/replace-agent-with-workflow` → main
- [ ] Context overflow UX: the error message is clear but users need to know
      which model to switch to. Consider printing estimated token count and
      suggesting a model in the error message.
- [ ] `bo list` shows "The Rust Programming Language" for all Rust Book pages —
      title extraction issue (known, tracked in features.md as "Improve extracted
      titles when UI chrome pollutes document metadata")
- [ ] Integration tests in `tests/integration_compile.rs` still reference
      the old agent behavior implicitly (they pass because cmd_compile signature
      is unchanged, but test names/comments could be updated)
- [ ] Prompt quality: gpt-4.1-mini still produced one hallucinated leaf filename
      and one single-leaf branch on the 53-doc corpus. Validation caught both,
      but prompt refinement can reduce wasted output tokens.

## Decisions deferred

- Leaf summary field for context-window scaling (features.md)
- `bo config set` command for compile_model (features.md)
- Chunking/map-reduce rejected in favor of summaries (see ADR discussion)
