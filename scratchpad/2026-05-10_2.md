# Session: 2026-05-10 (2)

## Refactor: unit tests restructuring

Branch: `refactor/unit-tests-restructuring`

### What was done

1. **Moved all inline unit tests to `src/tests/`** (22 source files → 23 test files)
   - Used `#[cfg(test)] #[path = "..."] mod tests;` pattern
   - Preserves `super::*` access to private internals
   - Source files shed ~4500 lines of inline test code

2. **Extracted `seed` and `raze` into `src/cli/seed.rs` and `src/cli/raze.rs`**
   - Previously private functions trapped in main.rs
   - Now independently testable modules with their own error types
   - `main.rs`: 1342 → 740 lines

3. **Decoupled seed/raze from HOME env var**
   - `seed()` and `raze()` now accept `config_path: &Path` parameter
   - Unit tests use temp paths directly — no `set_var("HOME")`, no `#[serial]`
   - Full parallel execution, zero flake risk

4. **Moved CLI integration tests to proper integration test files**
   - JSON envelope tests → `tests/integration_json.rs` (runs against compiled binary)
   - Only one tiny private-fn test remains inline in main.rs
   - Removed `serial_test` dependency from unit tests entirely

### Final state

- 291 tests passing, 0 failures, 27 ignored (network-dependent)
- No source file over 740 lines
- Unit tests fully parallel (no global state)
- Integration tests exercise the real binary via `Command::new`

### Structure

```
src/
  main.rs          (740)  — CLI orchestration
  cli/             — command modules (collect, compile, list, search, show, seed, raze, json)
  domain/          — pure types
  engine/          — business logic
  adapters/        — external services
  tests/           — unit tests (23 files)
tests/
  integration_cli.rs
  integration_json.rs
  integration_compile.rs
  integration_network.rs
  integration_search.rs
```
