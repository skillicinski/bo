# 2026-05-06

Unstructured capture. Move formulated feature candidates to `scratchpad/features.md`.

## Notes

- Established scratchpad workflow:
  - `scratchpad/YYYY-MM-DD.md` for loose notes while working.
  - `scratchpad/features.md` for tickable, somewhat formulated feature candidates.
  - `specs/<branch>/` for Speckit-promoted specs.
- Default promotion scope: one feature candidate per spec.

## Collection dogfood observations

- X/Twitter collection:
  - URL: `https://x.com/lifeof_jer/status/2048103471019434248`
  - Result: command exits ok, but collected X's JS-disabled/error shell instead of tweet content.
  - Possible framing: silent junk collection should become either useful extraction or explicit unsupported/failure.
- OpenReview collection:
  - URL: `https://openreview.net/forum?id=OAudWSf7aH`
  - Result: command exits ok, but collected mostly OpenReview shell/footer instead of paper content.
  - Possible framing: detect low-value shell pages, or add source-specific extraction.
- Rust blog redirect:
  - URL: `https://blog.rust-lang.org/2015/05/11/traits.html`
  - Result: collected a redirect page (`Redirect`, `Click here to be redirected`) instead of target content.
  - Possible framing: follow deterministic meta/client-side redirects before extraction.
- Rust book title extraction:
  - URLs: `https://doc.rust-lang.org/book/ch04-00-understanding-ownership.html`, `https://doc.rust-lang.org/book/ch16-00-concurrency.html`
  - Result: title extracted as `Keyboard shortcuts`; body contains some real chapter content.
  - Possible framing: improve title selection for documentation pages, likely prefer article/chapter heading over nav/UI text.

## Collection issue analysis

- Core issue: `collect` treats `HTTP 2xx + HTML content-type + trafilatura body over 50 chars` as success. That is too weak; boilerplate, redirect stubs, JS-disabled shells, and app footers can all satisfy it.
- Fetch layer only follows HTTP redirects. It does not follow deterministic HTML/JS redirect stubs such as Rust blog's `window.location.replace(...)` / `meta refresh` page.
- Extraction layer trusts generic trafilatura output as the document. It does not classify low-value output or fall back to structured metadata/app data.
- Source-specific observations:
  - Medium: real fetch/access failure; Cloudflare challenge returns `403`, so current failure is expected unless using a different fetch strategy or explicit unsupported/blocked classification.
  - X/Twitter: fetch can return a large HTML app/error shell; visible extracted text is the JS-disabled page, not tweet content. Needs source adapter or explicit unsupported/blocked detection.
  - OpenReview: useful title/abstract are present in meta/citation tags, but visible extraction returns shell/footer. Needs metadata/API fallback or adapter.
  - Rust blog: deterministic redirect stub; should be resolved before extraction.
  - Rust book/mdBook: HTML has correct `<title>` and `<main><h1>`, but extractor chooses `Keyboard shortcuts` from UI chrome. Needs DOM cleanup/title heuristic for docs pages.

## Reject-low-value implementation dogfood

- Dogfood after implementation: `39 ok, 3 failed`.
- Expected explicit rejections:
  - Rust blog redirect stub: `was not collected: redirect stub`.
  - OpenReview shell/footer extraction: `was not collected: boilerplate-only content`.
  - X/Twitter JS shell: `was not collected: JS-rendered content`.
- Medium URL collected successfully in the latest run; earlier 403/block behaviour remains covered by status and Cloudflare fixture tests. Follow-up check: `curl -A bo/0.1` still receives a Cloudflare `403`, while `target/debug/bo collect` via reqwest currently succeeds, so this appears to be client/CDN bot-gating variance rather than an intentional bo recovery. Return later: understand whether reqwest vs curl differences are headers, HTTP/2/TLS fingerprinting, cookie state, region/edge, or timing/bot-score variance.
- Rust book pages still collect, preserving the decision not to reject useful bodies solely because title extraction returns `Keyboard shortcuts`.
- A first dogfood pass falsely rejected `https://addyosmani.com/blog/good-spec/` as a redirect stub due bundled `window.location.href` JavaScript; redirect detection was tightened and the URL now collects.
